version: '3.8'

services:
  redis:
    image: redis:7.0.11-alpine
    container_name: redis
  django:
    container_name: django
    build:
      context: ./manager
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./manager:/usr/src/app
    ports:
      - "8001:8000"
    env_file:
      - ./manager/.env
    depends_on:
      - redis
  celery:
    container_name: celery
    build:
      context: ./manager
    command: celery --app=manager worker -l INFO
    volumes:
      - ./manager:/usr/src/app
    env_file:
      - ./manager/.env
    depends_on:
      - redis
  beat:
    container_name: beat
    build:
      context: ./manager
    command: celery -A manager beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    volumes:
      - ./manager:/usr/src/app
    depends_on:
      - redis
      - django
